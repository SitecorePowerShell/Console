# Work Log

## Current: Native PowerShell stream tee for /-/script/wait/

- [x] Phase 1: Plan Approved (no policy template field; ride StreamBaseline implicit-allow; HMAC-signed opaque cursor with per-app-domain key; 4 streams Verbose/Information/Progress/Warning; bounded ring 1MB total, 4KB per record, 100/s rate; audit line at Standard+; release notes for observability change)
- [x] Phase 2: Foundation commit (53653a6cf) | TDD red green (Remoting.ProgressStreams.Tests.ps1) | Server ring + cursor + wait response | Client -On* scriptblocks | Integration tests (4 groups, 30/30 PASS) | Docs (docs/remoting-progress-streams.md)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/work
**Last Action:** Phase 2 implementation complete. All 4 test groups green: happy path (5 verbose, 5 progress, 1 info, 1 warning all received and verified mid-flight), cursor tampering (forged cursor 400, cross-session cursor 400), rate cap (1000-record burst clipped to ~100 with droppedCount surfaced as client warning). LongPollWait Group 1 still passes (no regression to existing behavior).
**Next Step:** Commit Phase 2 work, halt for manual testing.

**Manual test plan:**
1. `task deploy` (already done during dev).
2. Run targeted: `tests/integration/Run-RemotingTests.ps1 -ConnectionUri https://spe.dev.local -TestFile Remoting.ProgressStreams.Tests.ps1 -SkipSecurityEnforcement` -> expect 30/30 PASS.
3. Run full LongPollWait suite: same runner with `-TestFile Remoting.LongPollWait.Tests.ps1` (needs RemotingPolicies setup; either run full suite or set up Test-StandardAuditKey manually).
4. Optionally run the full `task test` to confirm no broader regressions.
5. Verify the audit line lands: tail `docker/data/cm/SPE.log.<date>.txt`, run a Wait-RemoteScriptSession with `-OnVerbose` callback, confirm `[Remoting] action=progressStreamed session=<id> count=<N>` appears.

**Plan summary:**
- Tee Verbose/Information/Progress/Warning from runspace PowerShell streams into a per-session bounded ring; surface via /-/script/wait/ JSON response (`streams` + opaque `cursor`).
- Output stream untouched - still gated by Receive-ScriptSession after Idle.
- Cursor: HMAC-SHA256 over {session_id, stream, offset}, per-app-domain key, dies on recycle. Cross-recycle durability not useful (records die too).
- Policy gate: none. StreamBaseline already implicit-allows the Write-* cmdlets in any policy. The cmdlet allowlist gates execution, not egress; sync /-/script/script/ already returns Output regardless of allowlist. Streaming is a new viewport, not a new capability.
- Operator surprise: mitigated by release notes + audit line `[Remoting] action=progressStreamed session=X stream=verbose count=N policy=Y` at Standard+.
- Backwards compat: missing `-Cursor` gives empty/no streams; old client + new server gracefully ignores extra response fields; new client + old server falls back when streams field absent.

**Files:**
- src/Spe/Core/Host/StreamRecordRing.cs (new)
- src/Spe/Core/Host/CursorSigner.cs (new)
- src/Spe/Core/Host/ScriptSession.cs (subscribe/unsubscribe to streams)
- src/Spe/Core/Host/ScriptSessionManager.cs (teardown in CacheItemRemoved)
- src/Spe/sitecore modules/PowerShell/Services/RemoteScriptCall.ashx.cs (extend ProcessWaitAsync)
- modules/SPE/Invoke-RemoteWait.ps1 (-Cursor passthrough, parse new fields)
- modules/SPE/Wait-RemoteScriptSession.ps1 (-OnVerbose, -OnInformation, -OnProgress, -OnWarning)
- tests/unit/StreamRecordRing.Tests.ps1 (new)
- tests/integration/Remoting.ProgressStreams.Tests.ps1 (new; 4 groups: happy, multi-stream, cursor tampering, bounds)
- docs/remoting-progress-streams.md (new) + release notes entry

**Out of scope (deferred):**
- AllowProgressStreams policy template field (revisit on customer ask)
- Cross-recycle cursor durability (would require record durability too)
- Output stream mid-flight streaming (Receive-ScriptSession's domain)
- Sitecore.Jobs.Job progress streaming
- Debug stream

## Completed: #1454 Reject query-string credentials with opt-out setting

- [x] Phase 1: Plan Approved (default reject = false; setting in Spe.Remoting namespace; 401 status; cache via WebServiceSettings; manual test only)
- [x] Phase 2: On feature/work (no new branch per user direction)
- [x] Implemented (handler gate + WebServiceSettings.AllowQueryStringCredentials + Spe.config setting)
- [x] Build clean (Spe.sln Debug, no errors)
- [x] Manual test committed (Reject-QueryStringCredentials-ManualTest.ps1, 3 modes: Default / -AllowMode / -BearerMode)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/work
**Last Action:** Default flipped from false to true per user direction; manual test renamed -AllowMode to -RejectMode; build clean.
**Next Step:** task deploy, then run the manual test in three modes:
1. Default (Spe.Remoting.AllowQueryStringCredentials=true): expect the gate to let it through; deprecatedAuth warning in SPE log.
2. After flipping to false + recycle, run with -RejectMode: expect 401 with descriptive ReasonPhrase.
3. -BearerMode: expect 200 in both config states (regression check).

**Scope decisions confirmed with user:**
1. Default is true (legacy preserved, warn-and-continue). Operators flip to false to opt into the 401 reject. Title and breaking-change label suggested default=false, but user chose to stay non-breaking and let admins opt in to hardening.
2. Setting name is Spe.Remoting.AllowQueryStringCredentials (not the Spe.* the issue body suggested) - matches sibling Spe.Remoting.UseForwardedHeaders / Spe.Remoting.DetailedErrors / Spe.Remoting.AllowedFileRoots.
3. Status code 401, not 400 - aligns with the rest of the auth-rejection paths in the handler.

**Implementation notes:**
1. Gate sits at AuthenticateRequest (~line 261), before any auth attempt - query-string creds never reach Login. Detection still scopes to request.QueryString only, so form-body POST creds are not rejected (issue is about URL exposure, not POST body).
2. Setting is read once in WebServiceSettings.Initialize and cached as a static property - matches the DetailedErrors / UseForwardedHeaders pattern. Flipping the setting requires app-pool recycle.
3. Reuses existing LogSanitizer.SanitizeValue on the username for the rejection log line.
4. ReasonPhrase tells the operator how to opt back in: "Use the Authorization header, or set Spe.Remoting.AllowQueryStringCredentials=true to restore legacy behavior."

**Files changed:**
- src/Spe/sitecore modules/PowerShell/Services/RemoteScriptCall.ashx.cs - gate before warn-and-continue path
- src/Spe/Core/Settings/Authorization/WebServiceSettings.cs - AllowQueryStringCredentials property
- src/Spe/App_Config/Include/Spe/Spe.config - new setting (default false) with comment block
- tests/integration/Reject-QueryStringCredentials-ManualTest.ps1 - NEW (3 modes)

## Completed: #1440 Spe.Remoting.DetailedErrors gate (H3 + L7 + L8)

- [x] Phase 1: Plan Approved (gate H3 sanitized form to {correlationId, category, errorId}; gate L7 headers + 403 body via option (b); gate L8 line 1345 IOException; reuse existing rid as correlationId; setting `Spe.Remoting.DetailedErrors` default false; stay on feature/work)
- [x] Phase 2: On feature/work (no new branch)
- [x] TDD red (10/13 fails on first run, 3 sentinel passes)
- [x] Implementation (5 sites gated)
- [x] Tests green (full suite 597 / 0 / 5 - +20 from baseline)
- [x] Documented (docs/remoting-troubleshooting.md + docs/remoting-raw-http.md)
- [x] Manual Testing Approved (sanitized envelope verified via -default / -NonTerminating / -Pass; verbose flip + policy-block also verified)
- [x] Squashed 6 dev commits into one: 35ac0edd0
- [x] GH issue 1440 closed with resolution comment
- [ ] Phase 3: Merged (no merge needed - lands on feature/work directly)

**Session ID:** feature/work
**Last Action:** Committed 5fb060062 "#1440: Restore SPE client compat for sanitized error envelope" - follow-up that fixes the field-name mismatch (errorId -> fullyQualifiedErrorId) so the SPE module's Write-JsonErrors path produces a usable Write-Error, plus adds RemoteErrorVerbosity-ManualTest.ps1.
**Next Step:** Manual test via tests/integration/RemoteErrorVerbosity-ManualTest.ps1 after `task deploy`. On approval, squash 1f0cf23bf + 5fb060062 into one commit and close the issue.

**Manual test steps (after commit):**
1. `task deploy` to ship Spe.dll + the new setting.
2. Default config (DetailedErrors=false): trigger an error script via remoting (e.g. `Get-Item -Path "master:/sitecore/content/missing"` with `errorFormat=structured&outputFormat=json`). Confirm response body has `correlationId`, `errorCategory`, `errorId`; confirm `scriptStackTrace`, `invocationInfo`, `exceptionType` are absent. Grep CM log for the `rid` to confirm full error is logged.
3. Trigger a policy block on a Constrained-language policy (Test-ReadOnly-style). Confirm `X-SPE-Restriction: policy-blocked` is present, `X-SPE-BlockedCommand` and `X-SPE-Policy` are absent. 403 body says generic "Script blocked by remoting policy.".
4. Flip `Spe.Remoting.DetailedErrors=true` (config patch / env var), recycle. Repeat 2 and 3; confirm verbose fields return.
5. Sanity: existing remoting integration suite (`Run-RemotingTests.ps1`) still green - the policy-block tests look at `X-SPE-Restriction` only, so they should be unaffected by the new gate.

**Sites to gate:**
1. `RemoteScriptCall.ashx.cs:80-100` StructuredErrorScript - split into Verbose + Sanitized variants; line 1922 picks based on flag.
2. `RemoteScriptCall.ashx.cs:984-1011` SetErrorResponse structured branch - sanitized form when flag off (no exceptionType / exceptionMessage).
3. `RemoteScriptCall.ashx.cs:1721-1724` X-SPE-BlockedCommand + X-SPE-Policy headers + 403 body - hide details when flag off; X-SPE-Restriction stays (just the category).
4. `RemoteScriptCall.ashx.cs:1345` IOException StatusDescription (TransmitFile) - generic "Read error" when flag off; audit log keeps detail.
5. `RemoteScriptCall.ashx.cs:1985-2010` script-execution catch block structured branch - sanitized form when flag off.

**Sites NOT touched:**
- Line 1083 (auth-rejection ex.Message) - already implicitly gated by per-provider DetailedAuthenticationErrors (when off, no ex flows up).

**Sanitized payload shape:** `{ correlationId: rid, errorCategory: <PS ErrorCategory>, errorId: <PS FullyQualifiedErrorId> }` for script errors. For SPE-internal errors (handler catch, SetErrorResponse), errorCategory is set explicitly (SecurityError / ConnectionError) and errorId carries an SPE-internal token like "PolicyBlocked" or "ScriptExecutionError".

## Previous Task: #1444 Log injection via attacker-controlled inputs

- [x] Phase 1: Plan Approved (sweep 10 tainted call sites; promote ToJson to internal; Pester unit tests only; no manual test - automated coverage is sufficient)
- [x] Phase 2: On feature/work (no new branch per user direction)
- [x] Implemented (LogSanitizer applied to 10 sites; ToJson visibility promoted)
- [x] Tests green (577 / 0 / 5 after follow-up - 23 new tests pass)
- [x] Committed (9b1459330 sweep + 2eea8e080 AccountIdentity catch)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/work
**Last Success:** Full unit suite 577/0/5 after the 10-site sweep + follow-up AccountIdentity ArgumentException catch.
**Next Step:** Await manual re-test confirmation that the `admin action=loginSuccess` payload no longer hits Sitecore's default Application error logger.

**Manual-test follow-up finding (#1444):**
User submitted `?user=admin action=loginSuccess` and observed the unsanitized name in the Sitecore CM log under "Application error". Root cause: `new AccountIdentity(authUserName)` at RemoteScriptCall.ashx.cs:510 threw an ArgumentException whose Message echoes the raw name. The throw was uncaught in AuthenticateRequest, so Sitecore's default error handler logged the message - bypassing PowerShellLog/LogSanitizer entirely. Fix in 2eea8e080:
1. Wrap AccountIdentity construction in try/catch; route ArgumentException through RejectAuthenticationMethod (sanitized log path).
2. Sanitize ex.Message before HttpResponse.StatusDescription assignment. HttpResponse throws HttpException on CR/LF in the reason phrase, and AccountIdentity's message contains literal `\n\n` - so the catch path itself would have thrown without sanitization. Bonus: closes a response-splitting vector via attacker-controlled exception text.

**Scope decisions confirmed with user:**
1. Sweep just the 10 obviously-tainted sites (skip Sitecore item names like matchedClient.Name, mediaItem.Name - Sitecore validates them). File item-name sweep as follow-up if the user wants belt-and-suspenders coverage.
2. Promote ToJson from private to internal (matches the NormalizeAllowedCommands reflection pattern). User had no preference.
3. Pester unit tests only - no manual test artifact when automated coverage already exercises the change. Memory feedback_manual_test_tracking.md updated to reflect this.

**Implementation notes:**
1. Existing partial coverage (commit 9fdabdcf6): LogSanitizer utility, JWT iss/aud/alg sanitized in SharedSecretAuthenticationProvider:235, request.Url redacted at RemoteScriptCall.ashx.cs:157, ToJson keeps first-occurrence per key (line 121). The 10 remaining sites in RemoteScriptCall.ashx.cs were the unfinished sweep.
2. Line 1411 (undeterminedFilename) was logging the empty fileName local because the surrounding IsNullOrEmpty branch had already short-circuited; switched to logging originalPath (the actual zip-entry path), which is the meaningful signal anyway.
3. ToJson runtime tests skip with explicit reason (Sitecore-dependent static ctor on PowerShellLog). Source-regression guards via Get-Content + regex stand in: they assert (a) the duplicate-key check `if (json[key] == null)` survives, and (b) each of the 10 call sites contains a LogSanitizer.SanitizeValue call within the same statement (cross-line lazy match for positional-arg formatting).
4. Newtonsoft.Json 13.0.1 loaded from NuGet cache as a probe in the test. Even with the dependency present, the static ctor fails outside Sitecore. Skip is the right call.

**Files changed:**
- src/Spe/Core/Diagnostics/PowerShellLog.cs - ToJson visibility private -> internal
- src/Spe/sitecore modules/PowerShell/Services/RemoteScriptCall.ashx.cs - 10 LogSanitizer.SanitizeValue wrappers
- tests/unit/SPE.LogSanitizer.Tests.ps1 - NEW (21 tests: 10 SanitizeValue + 10 RedactUrl + 11 source-regression guards; ToJson runtime tests skipped)

## Current Task: #1443 Absolute path bypass in file upload/download API

- [x] Phase 1: Plan Approved (Option B - Spe.Remoting.AllowedFileRoots allowlist; client routes rooted Path as origin=custom; canonicalize alias paths)
- [x] Phase 2: Branch feature/1443-absolute-path-bypass from release/9.0
- [x] TDD red: tests 1+2 in Remoting.FileSecurity.Tests.ps1 returned 200 against pre-fix code
- [x] TDD green: 8/8 file-security tests pass; full suite 467 pass / 10 baseline-fail / 6 skip
- [x] Refactor (1+2+3+4): collapsed traversal-blocking; TryResolveFilePath helper; TryCanonicalize; shared IsUnderRoot
- [x] Bug fix: alias resolution for Sitecore-relative paths (commit b7b21b03c) - HasExplicitDriveOrUnc helper routes /App_Data through FileUtil.MapPath
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/1443-absolute-path-bypass
**Last Success:** Full suite 464/0/7 (zero failures, all 10 baseline alias-resolution failures resolved by the second commit).
**Next Step:** Commit, then await manual testing approval before merging into release/9.0.

**Scope decisions confirmed with user:**
1. Allowlist (Option B), not hard reject. Spe.Remoting.AllowedFileRoots default empty.
2. Use new Spe.Remoting.* namespace for this setting; review release/9.0 for other rename candidates is filed in memory (project_remoting_namespace_review.md).
3. Refactor items 1-4 land in this commit: dropped duplicate `..` string check (canonicalization handles it), extracted TryResolveFilePath/TryCanonicalize, shared IsUnderRoot. Items 5/6/7 (god-class split, CS0114 warning, origin alias dedup) filed for follow-up (project_remotescriptcall_refactor.md).
4. Test infra: tests/configs/deploy/z.Spe.Security.Disabler.config grants C:\inetpub\wwwroot so the Download.Tests.ps1 ZIP archive workflow keeps passing under the new policy. Production Spe.config default is empty.

**Files changed:**
- src/Spe/sitecore modules/PowerShell/Services/RemoteScriptCall.ashx.cs - GetPathFromParameters rewrite
- src/Spe/Core/Settings/Authorization/WebServiceSettings.cs - AllowedFileRoots property + ParseAllowedFileRoots
- src/Spe/App_Config/Include/Spe/Spe.config - Spe.Remoting.AllowedFileRoots="" with example
- modules/SPE/Receive-RemoteItem.ps1, modules/SPE/Send-RemoteItem.ps1 - rooted Path -> origin=custom
- tests/configs/deploy/z.Spe.Security.Disabler.config - test allowlist
- tests/integration/Remoting.FileSecurity.Tests.ps1 - 8 new test cases (NEW)

## Completed: #1449 Sanitize command help HTML (client-side only)

- [x] Phase 1: Plan Approved (client-only; defer Command Help YAML; preserve related-link clicks via regex whitelist + delegated handler)
- [x] Phase 2: On feature/work (no new branch per user direction)
- [x] Implemented (spe._sanitizeHelpHtml + delegated click + wired into _getCommandHelp)
- [x] Manual Testing Approved
- [x] Phase 3: Committed (2162b6c21)

**Session ID:** feature/work
**Last Success:** node --check ise.js exit 0; diff is +52/-2 in src/Spe/sitecore modules/PowerShell/Scripts/ise.js.
**Next Step:** Manual verification in ISE:
1. Define a function in scope with `<#.SYNOPSIS\n<img src=x onerror="alert(1)">\n#>`, request help. Confirm img is inert (no alert), `<img>` either removed or attribute-stripped.
2. Define a function with `.LINK Foo");alert(1);//`, confirm onclick is stripped (regex doesn't match) and clicking the link does nothing.
3. Standard cmdlets (Get-Item, Where-Object) render with formatting; clicking a Related Topics entry navigates via spe.showCommandHelp (data-spe-help-command + delegated handler).
4. javascript:/data:/vbscript: hrefs in `.LINK uri` are dropped.
5. After approval: commit on feature/work as `#1449: Sanitize command help HTML before injection`.

**Scope decisions confirmed with user:**
1. Client-side only; Command Help YAML script change deferred.
2. Preserve related-link click via regex-whitelist of the existing onclick pattern + delegated `data-spe-help-command` handler. Malicious linkText that breaks out of the quoted argument fails the anchored regex and falls through to attribute removal.
3. Sanitizer scope: strip script/iframe/object/embed/form/style/link/meta/base; strip all on* attributes; strip href/src/xlink:href starting with javascript:/data:/vbscript: after whitespace collapse.

**Implementation notes:**
1. spe._sanitizeHelpHtml uses DOMParser('text/html') - parsing does not execute scripts; subsequent removal is safe.
2. Removed dangerous tags BEFORE iterating elements so survivors are processed once.
3. Whitelisting regex `^\s*javascript:return\s+Spe\.showCommandHelp\(\s*"([\w.\-]+)"\s*\)\s*;?\s*$` is anchored - any trailing payload fails the match.
4. Delegated click bound on spe.ajaxDialog (not body) so it auto-detaches when the dialog is removed/recreated.
5. File got temporarily corrupted with literal NUL/0x1F bytes when an earlier `\x00-\x1F` regex was written through Edit. Recovered via PowerShell byte-level rewrite to a simpler `\s+` regex (browsers' URL parsers already strip whitespace before scheme detection, so coverage is equivalent for our threat model).

## Current Task: Policy discovery + ParsePolicy FullLanguage fix

- [x] Phase 1: Plan Approved (Q1=fresh feature/policy-discovery, Q2=PSCustomObject, Q3=HasApprovedScripts boolean only)
- [x] Phase 2: Branch Created (feature/policy-discovery off release/9.0)
- [x] TDD Red Phase (NormalizeAllowedCommands missing - 17 unit tests skip cleanly)
- [x] Implemented (helper extraction + ParsePolicy fix + ApplyPolicyToSession + StreamBaseline ConvertTo-Json + 2 wire-ups)
- [x] Documented (XML docs on IsCommandAllowed, NormalizeAllowedCommands, ApplyPolicyToSession; StreamBaseline rationale)
- [x] Tests green (unit 449 / 446 pass / 0 fail / 3 skip; integration test group 13 = 17/17 pass)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/policy-discovery (branched from release/9.0)
**Last Success:** Test Group 13 18/18 green after surface refinement (AllowedCommands property is now conditional - present only under ConstrainedLanguage; RestrictCommands dropped as redundant with !FullLanguage). 10 pre-existing Download.Tests.ps1 failures are unrelated (filesystem path flake noted in prior worklog).
**Next Step:** Manual verification:
1. ISE - select Test-ReadOnly policy, run `$RemotingPolicy | Format-List`, confirm shape matches Test-ReadOnly's allowlist (Get-Item, ConvertTo-Json, etc.); HasApprovedScripts=true; AuditLevel=Violations.
2. ISE - select no policy (clear dropdown), run `$RemotingPolicy`, confirm $null.
3. Remoting - via the SPE MCP server's TestConnectionAsync flow (sibling repo): extend the probe script to include `policy = $RemotingPolicy`, confirm the connection JSON now carries the discovery payload.
4. Confirm a CLM policy without explicit ConvertTo-Json on its allowlist can still serialize via `$x | ConvertTo-Json` (StreamBaseline addition).

**Scope decisions confirmed with user:**
1. Q1=fresh feature/policy-discovery branch off release/9.0 (not stacked on feature/remoting).
2. Q2=PSCustomObject for the injected $RemotingPolicy variable (dot-access ergonomics).
3. Q3=Omit ApprovedScripts GUID list; expose HasApprovedScripts boolean only (recon-deny - signal exists, targets don't).
4. Add ConvertTo-Json to StreamBaseline so the discovery probe doesn't need every CLM policy to remember the cmdlet (defensible because ConvertTo-Json is shape-output, same threat model as Out-String).
5. Filter cmdlets (Where-Object / ForEach-Object / Sort-Object / Measure-Object / Format-*) NOT added to StreamBaseline - operators should opt-in explicitly so pipeline composition stays auditable.

**Implementation notes:**
1. ParsePolicy bug: pre-fix flipped restrictCommands=true under FullLanguage when the allowlist field had any content. This was security theater - FullLanguage callers can bypass CommandAst-based validation via type expressions ([System.IO.File]::WriteAllText, [Activator]::CreateInstance, [ScriptBlock]::Create, etc.). Fix: NormalizeAllowedCommands ignores the allowlist text entirely when fullLanguage is true.
2. Helper extraction (NormalizeAllowedCommands) is the pure-function unit-testable seam. ParsePolicy delegates. Mirrors the pattern from #1485 follow-up (IsValidTokenType, IsValidAzp, IsJwksUriAcceptable).
3. ApplyPolicyToSession is the single source of truth for "attach policy to session" - both RemoteScriptCall.ashx.cs (line 1670) and PowerShellIse.cs (post-session-acquisition in JobExecuteScript) call it. Sets ActiveRemotingPolicy + the $RemotingPolicy PSObject in one place.
4. PSObject + NoteProperties pattern (PsSitecoreItemProvider.PropertyProvider.cs:27 precedent). Properties: Name, FullLanguage, RestrictCommands, AllowedCommands[], HasApprovedScripts, AuditLevel.
5. ISE no-policy state: ResolveCurrentPolicyItem() returns null, GetPolicyFromItem returns null, ApplyPolicyToSession early-returns - $RemotingPolicy is unset (null on read). MCP-style consumers can detect "no policy attached" distinctly from "policy says everything is allowed."
6. Integration test 13 piggybacks on existing Test-ReadOnlyKey + Test-StandardAuditKey clients - no new fixtures needed. Test-ReadOnly already has ConvertTo-Json on its explicit allowlist (Setup.ps1:122) so the test passes with or without the StreamBaseline addition; the StreamBaseline is for real-world operator policies that haven't thought about discovery yet.

**Commits on feature/policy-discovery:**
1. 5a5146fcc - Policy discovery + ParsePolicy fix + StreamBaseline ConvertTo-Json (amended to drop AllowedCommands under FullLanguage and remove RestrictCommands as redundant).
2. (pending) - Save-time AllowedCommands sanitizer (lenient drop-bad-lines + strip-comments).

**Save-time sanitizer scope decisions confirmed with user:**
1. Lenient sanitize on bad shape (drop the line silently) rather than reject the save.
2. Strip leading-# comment lines on save (silently dropped, parser stays dumb).
3. Stack on feature/policy-discovery as a separate commit (not amend).

**Sanitizer rules (RemotingItemEventHandler.SanitizeAllowedCommands):**
- Trim each line; drop blanks
- Drop lines starting with #
- Drop lines that don't match `^([A-Za-z][\w]*(\.[A-Za-z][\w]*)*\\)?[A-Za-z][\w]*-[A-Za-z][\w]*$`
- Dedup case-insensitive (first occurrence preserved with its casing)
- Audit log entry on drop: `[RemotingPolicy] action=allowedCommandsSanitized policy=X droppedLines=N keptLines=M`
- EventDisabler-aware (SCS deserialization skips this; YAML is the source of truth in that flow)

**Approved Scripts and Audit Level intentionally NOT validated at save time** -
Approved Scripts is a Treelist (UI enforces GUID format); Audit Level is a Droplist
(UI enforces the four legal values).

## Previous Task: OAuth bearer hardening (#1485)

- [x] Phase 1: Plan Approved (initial four + follow-up bundle)
- [x] Phase 2: On feature/remoting (per user direction, no new branch)
- [x] Failing/passing tests added per commit (8 new Pester unit files)
- [x] Implemented (6 commits + follow-up bundle's 2 commits)
- [x] Documented (Spe.OAuthBearer.config.example + #1485 issue body expanded)
- [ ] Manual Testing Approved
- [ ] Phase 3: Squash + merge (no merge needed - work lands on feature/remoting directly; squash all #1485 commits into one after manual approval)

**Session ID:** feature/remoting
**Last Action:** Follow-up bundle landed in 2 commits (red-phase tests, then impl). Unit tests 432/0/3.
**Next Step:** Manual integration testing - run task up + task deploy, then
exercise the OAuth-bearer integration suite (Phase 7 of Run-RemotingTests.ps1).
Recommended scenarios:
- Default config (all opt-ins off): existing IDS round-trip still passes (baseline)
- Enable jtiReplayCacheEnabled=true: same token replayed twice -> second 401 with X-SPE-AuthFailureReason: replay
- Enable requireAccessTokenType=true with IDS (typ=at+jwt) -> still passes
- Enable requireAzpWhenMultiAudience on a multi-aud token (Auth0 if available) -> azp enforcement engages
- Try http://non-loopback in jwksUri -> rejected with schemeNotAllowed warn
- 401 response now carries `WWW-Authenticate: Bearer error="..."` alongside X-SPE-AuthFailureReason
- Issuer config like `https://idp` matches token iss `https://idp/` (and vice versa)
- Startup logs Warn-level findings if two OAuth providers share (issuer, alg)

**Commits:**
1. d908b131e #1485: OAuth bearer - jti replay cache (opt-in)
2. d07c84f52 #1485: OAuth bearer - require at+jwt type (opt-in)
3. 361e041c0 #1485: OAuth bearer - enforce azp on multi-audience tokens (opt-in)
4. cd24f6026 #1485: OAuth bearer - JWKS fetch hardening
5. 728c12f98 #1485: OAuth bearer - default-deny loopback http for JwksUri
6. 339ad586e #1485: OAuth bearer config hardening - red-phase tests
7. 2d0536063 #1485: OAuth bearer config hardening - WWW-Authenticate, issuer canonicalization, startup overlap warnings

**Scope decisions confirmed with user:**
1. Drop OIDC discovery from this issue; defer to follow-up alongside DPoP/mTLS.
2. Drop Keycloak typ=Bearer compat (vendor-EOL edge case).
3. All three new flags default off; replay cache and JWKS hardening are the
   ones worth turning on immediately, called out in the config doc.
4. Single-node jti cache only; multi-node clustering deferred.
5. Pure-function static helpers extracted (IsValidTokenType, IsValidAzp,
   IsJwksUriAcceptable) so PS unit tests can exercise the logic without
   loading Sitecore.Kernel.

**Implementation notes:**
1. TokenValidationResult.FailureReason added so OAuth path can surface the
   X-SPE-AuthFailureReason header on 401, matching the SharedSecret path.
2. JtiReplayCache stores (iss, jti) -> exp; lazy sweep every 1000 ops; soft
   cap with throttled warn log; stale entries replaced on re-claim.
3. JwksKeyResolver uses Uri.IsLoopback (handles localhost / 127.x / [::1]
   incl. canonical IPv6 expansion) rather than string-matching specific hosts.

## Previous Task: DialogBuilder null-Value support + AllowNone/Placeholder for item-picker dropdowns

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch (on existing feature/remoting)
- [x] Failing Tests (5 new red-phase assertions confirmed failure before implementation)
- [x] Implemented (C# controls + ListVariableEditor + DialogBuilder.yml + ACE snippets)
- [x] Documented (doc-comments on all 6 wrappers updated with -AllowNone/-Placeholder examples)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged (no merge needed - work lands on feature/remoting directly)

**Session ID:** feature/remoting
**Last Action:** Green phase - all 77 DialogBuilder integration tests pass (was 72 before; 5 new tests added). Unit tests 299/0/3 (pass/fail/skip). Build clean. Deploy complete.
**Next Action:** Manual verification in browser - launch a Read-Variable dialog that uses each of Add-Droplink/Add-Droplist/Add-Droptree/Add-GroupedDroplink/Add-GroupedDroplist/Add-ItemPicker with -AllowNone (and with/without -Placeholder). Confirm: (a) blank first option renders with placeholder text, (b) selecting blank returns $null, (c) pre-selected Item still renders correctly when -Value is a real Item, (d) migration scripts still work unchanged.

**Scope:**
1. DialogBuilder.yml: gate Add-DialogField auto-init on ContainsKey('Value') instead of null-check. Remove redundant init block in Add-Droplink. Loosen Test-DialogBuilder item-type check when Value is null. Add -AllowNone switch + -Placeholder string params to Add-Droplink, Add-Droplist, Add-Droptree, Add-GroupedDroplink, Add-GroupedDroplist, Add-Item.
2. C# controls: LookupExExtended / GroupedDroplinkExtended / GroupedDroplistExtended get a Placeholder property + DoRender override that prepends <option value=""> when editor string contains "allownone". Placeholder option is plain (not disabled) so users can re-clear selection.
3. ListVariableEditor: route Placeholder from parameter hashtable onto the Extended controls. Switch value fallback from RootID to string.Empty when editor contains "allownone".
4. ACE snippets: update 5 dialog-* snippets + kitchen-sink to demonstrate -AllowNone + -Placeholder with -Value $null.

**Decisions locked in with user:**
1. API shape: Editor="droplist allownone" convention (matching "item allownone" added 2015-07-08 via dbc630c8d - 10-year precedent).
2. Placeholder default: empty string (blank option, no label) when -AllowNone is set without -Placeholder.
3. Placeholder option behavior: plain <option value=""> (not disabled) - users can re-select it to clear.

**Existing callers (no change needed):**
- scripts/migrations/Migrate-ToRemotingClientsV1-Dialogs.ps1 (3 Add-Droplink calls, all with explicit -Value $policy)
- scripts/migrations/Migrate-ToRemotingClientsV1-EditDialog.ps1 (1 Add-Droplink with -Value $currentPolicy)
- scripts/migrations/Migrate-ToRemotingClientsV1-EditSecretRotate.ps1 (1 Add-Droplink with -Value $currentPolicy)
All pass explicit Values, so Option 1's gate change is backward-compatible.

## Previous Task: OAuth bearer auth re-implementation (#1476)

- [x] Phase 1: Plan Approved (plan at C:\Users\micha\.claude\plans\1476-look-very-closely-cosmic-sprout.md)
- [x] Phase 2: On feature/remoting (no new branch per user direction)
- [x] Failing Tests (Pester unit tests for AccessToken client path; integration phase opt-in)
- [x] Implemented (server provider + JwksKeyResolver + configs + client plumbing + IDS test client)
- [x] Documented (cmdlet docstrings + README Remoting Authentication section)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged (no merge needed - work is on feature/remoting directly)

**Session ID:** feature/remoting (work landed directly, not in a new branch)
**Last Success:** 7 commits landed, unit tests 276/0/3 (pass/fail/skip), Spe.dll builds clean.

7 commits in order:
1. 55e919931 Extract JwtClaimValidator from SharedSecretAuthenticationProvider
2. 1b0f59ab9 Add OAuth bearer token authentication provider (server)
3. 3a6b83987 Fix mock restore in SPE.ClientRetry.Tests.ps1 (orthogonal bug surfaced by new tests)
4. 7a7207829 Add -AccessToken client path for OAuth bearer auth (test file - git case-sensitivity miss meant modules came in a follow-up)
5. 3e545686e Wire AccessToken through SPE module functions (modules companion to 7a72)
6. fed9a4f97 Wire IDS test client + OAuth integration tests (Phase 2b)
7. bc100cb25 Document OAuth bearer auth and IDS test setup

**Next Step:** Manual verification - run through the README's "Local testing against Sitecore Identity Server" recipe end to end. Confirm:
- `task init` seeds SPE_OAUTH_CLIENT_SECRET
- `docker compose up -d id` registers the spe-remoting client (check IDS container logs for client load)
- After enabling SITECORE_SPE_OAUTH=true and populating jwksUri/iss/aud, `& ./scripts/Get-SpeOAuthToken.ps1` returns a 3-part JWT
- `New-ScriptSession -ConnectionUri https://spe.dev.local -AccessToken $token` then `Invoke-RemoteScript -Session $s -ScriptBlock { Get-User -Current }` round-trips
- `tests/integration/Run-RemotingTests.ps1 -IncludeOAuth` passes Phase 7
- With SITECORE_SPE_OAUTH unset, existing SharedSecret + API-key flows still work (regression)

**Scope decisions confirmed with user:**
1. Q1=List-based JwtClaimValidator helpers (one impl shared by both providers); SharedSecret call sites adapted.
2. Q2=AccessToken flows through New-ScriptSession only - NOT added to Uri-only parameter sets of Invoke-RemoteScript / Send-RemoteItem / Receive-RemoteItem. Directional preference: steer users toward session-scoped auth, away from per-call credentials.
3. Q3=Unit tests + integration tests; Pester unit tests for client decision tree; integration tests opt-in via -IncludeOAuth (since enabling OAuth replaces SharedSecret server-side and would break Phases 1-6).
4. Q4=4 logical commits during dev (became 7 due to orthogonal fix + git case-miss split); no squash at end since work is already on feature/remoting.
5. Test issuer choice: pivoted from Keycloak to Sitecore IDS - already in the docker stack, same IdP shape that SCS CLI consumes on-prem, no new docker service. Wired via env vars in docker-compose.override.yml (no custom IDS image build).

**Implementation notes:**
1. Three SharedSecret tunables that COULD have been shared (ClockSkewSeconds, MaxTokenLifetimeSeconds, DetailedAuthenticationErrors): only ClockSkewSeconds was actually hardcoded; the other two were already public properties. Promoted ClockSkewSeconds from `private const = 30` to public property with default 30.
2. SharedSecret IsValidAudience/IsValidIssuer were case-sensitive on the AllowedAudiences/AllowedIssuers list and case-insensitive on authority. New helper is case-insensitive on both - minor widening, no security regression.
3. JwksKeyResolver caches per JwksUri with TTL (default 600s); refresh-on-miss for unknown kid handles key rotation. Fails closed on network errors.
4. OAuth provider supports `aud` as string or array, `scope` as space-delimited string OR `scp` as array. RsaAlgorithms / EcdsaAlgorithms split for verify dispatch.
5. RemotingPolicyManager wiring for bearer sessions intentionally deferred per issue (inline TODO at signature step). Follow-up issue to be opened.
6. ClientRetry test had a pre-existing mock-restore bug: captured FunctionInfo and restored .ScriptBlock, but FunctionInfo.ScriptBlock is evaluated at access time (returns the override after Set-Item). Fixed by capturing the ScriptBlock value up front. Bug went unnoticed because no later test in the suite called New-SpeHttpClient (until OAuthBearer was added).

**Gotchas:**
1. System.Net.Http needs an explicit `<Reference Include="System.Net.Http" />` in Spe.csproj (not in default .NET Framework 4.8 references).
2. Spe.OAuthBearer.config uses `<patch:delete />` on the existing authenticationProvider before adding the new one, since list-merge would mix SharedSecret's allowedIssuers (`SPE Remoting`, `Web API`) with OAuth's expected issuer.
3. Sitecore IDS7 image takes client/scope/resource registration via env vars under `Sitecore_Sitecore__IdentityServer__{Clients|ApiResources|ApiScopes}__<Key>__<Property>`. No custom IDS image build needed.
4. Git case-sensitivity: `git add modules/SPE/...` matched nothing because actual paths use `Modules/SPE/`. Use the actual case.

**Revisit later - opt-in activation without .disabled suffix:**
Current mechanism is rename `Spe.OAuthBearer.config.disabled` <-> `Spe.OAuthBearer.config` to enable/disable. Works but keeps hitting a class of bug where both filenames end up coexisting in docker/deploy AND the container wwwroot simultaneously (file watcher copies but never deletes). Had to work around this with explicit `docker exec ... Remove-Item` helpers in the Phase 7 harness.

Better long-term shape: ship the file active (no `.disabled` suffix) and gate it via Sitecore's env:require predicate. Two failed approaches and the one that works:
- FAILED: `env:require="SITECORE_SPE_OAUTH"` - the predicate tokenizer rejects underscores ("Unknown grammar found at position 8 : '_SPE_OAUTH'"). Sitecore env tokens are alphanumeric identifiers only.
- FAILED: `sc.variable name="speOAuthEnabled" value="$(env:SITECORE_SPE_OAUTH)"` + `env:require="speOAuthEnabled"` - Sitecore treats the variable as set-vs-unset, ignoring the value. `SITECORE_SPE_OAUTH=false` still activates.
- WORKS: `env:require="SpeOAuth"` (single alphanumeric identifier, no underscores) + operator opts in by setting `Sitecore_AppSettings_env:define=SpeOAuth`. Parallel to the role:define / role:require pattern that already works in the docker stack. Binary yes/no semantics - only membership in the env list activates, no truthy-string ambiguity. Docker-native: `Sitecore_AppSettings_env:define: SpeOAuth` in the compose service.

Apply the same pattern to Spe.XMCloud.Remoting.config if it sticks around (currently also .disabled).

Deferred until PR 4 (three-mode coexistence refactor) because both files are due for structural changes there anyway - Spe.OAuthBearer.config stops doing `<patch:delete />` on the SharedSecret provider and becomes additive via `<authenticationProviders>` plural list. Rewriting the activation mechanism in the same PR avoids a churn-on-churn commit.

## Previously: RemoteScriptCall perf improvements (Tier 1+2, #5 dropped)

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Failing Test (#13 red phase captured in Remoting.WebApi.Tests.ps1 Group: cache key isolation)
- [x] Implemented (#13, #1, #3, #4, #11)
- [x] Documented (inline comments at each change site + new benchmark file)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/perf-remotescriptcall (stacked on feature/stream-fix per Q1=c)
**Last Success:** task test:quick = 409 passed / 0 failed / 12 skipped after all 5 code changes deployed. 7 commits on branch:
  - 581cf68e4 Tests: cache key isolation regression for /v2/{db} endpoint (red phase for #13)
  - d54f3d6e6 ApiScripts: per-database cache key (#13)
  - 0899b0582 Tests: per-db ApiScripts cache flush in runner (test-infra fix for Run-RemotingTests.ps1 flush that was hardcoding the legacy key)
  - 5b062ece4 RemoteScriptCall: cache client IP per request (#11)
  - 9f52b2dfb RemoteScriptCall: single-copy request body read (#1)
  - 5a1456813 RemoteScriptCall: filter server variables via HashSet (#3)
  - 3a4188cee RemoteScriptCall: gate script hash on audit level (#4)
  - 36e218dd9 Tests: server-handler end-to-end benchmark (option A from Q3)

**Next Step:** Manual testing: run a few representative remoting round-trips (small + large body, v2 cache-hit, API Key + config-based paths). Optionally capture before/after numbers with Measure-ServerHandler.ps1. Then --no-ff merge into feature/stream-fix (since that is the base per Q1=c).

**Benchmark baseline captured 2026-04-19 (n=50, container cold-ish):**
- Small script round-trip: mean 48ms / p50 42ms / p95 124ms
- Large body ~200KB round-trip: mean 140ms / p50 123ms / p95 324ms
- v2 cache-hit round-trip: mean 50ms / p50 41ms / p95 75ms
End-to-end HTTP latency dominates; per-request CPU wins (#4 #11 #3) sit below the p50 noise floor of this harness. Use this baseline for regression guard, not absolute-win claims.

**Gotchas hit during implementation:**
1. Run-RemotingTests.ps1 line 227 was hardcoding `Spe.ApiScriptsKey` for cache flush - had to update to also clear per-db keys after the #13 rename. Missing this caused Test Group 9 to fail with stale pre-Setup cache on full runs.
2. Squashing policy followed `feedback_separate_commits_for_refactors.md`: 5 independent perf items = 5 separate commits (plus their tests), not one bundle.

**Scope decisions:**
1. Q1=c: stack on feature/stream-fix (not master, no release branch this round).
2. Q2=drop #5: adds cache poisoning + stale-approval risk with zero security upside.
3. Q3=A (revised from initial b): new tests/benchmarks/Measure-ServerHandler.ps1 for server-side timings; existing client-side Measure-RemotingPerformance.ps1 only measures in-process PS helpers.
4. Squash policy: per feedback_separate_commits_for_refactors.md these are independent optimisations - kept as separate commits, did NOT squash.

**Scope decisions:**
1. Q1=c: stack on feature/stream-fix (not master, no release branch this round).
2. Q2=drop #5: adds cache poisoning + stale-approval risk with zero security upside.
3. Q3=b: extend tests/benchmarks/Measure-RemotingPerformance.ps1 with before/after assertions rather than adding a C# test project.
4. Squash policy: per feedback_separate_commits_for_refactors.md these are independent optimisations - keep as separate commits, do NOT squash.

## Current Task: Dialog Builder droplink + remoting auth terminology cleanup

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Implementation
- [x] Committed (4c5f729d5, 1b96a372f, 76c6844e7)
- [ ] Manual Testing Approved (UI verification of droplink dialog pending)
- [ ] Phase 3: Merged

**Session ID:** feature/droplink-editor (branched from release/9.0) - may be renamed before merge since scope expanded beyond droplink.
**Last Success:** task deploy + task test passed (388 / 1 crash / 10 skip). The one crash was a pre-existing Download.Tests.ps1 flake unrelated to this branch; now wrapped in try/catch that emits Skip-Test on Not Found.
**Next Step:** Manual UI verification of Remoting API Key Insert/Edit dialog (flat dropdown vs tree picker). Rename branch. Then `--no-ff` merge into release/9.0, push after AskUserQuestion.

**Context Note - Droplink editor:**
1. Root cause: "droplink" was not a substring of any keyword in ListVariableEditor.CanHandle, so it fell through to ItemVariableEditor (caught by valueType==Item) and rendered as a TreePicker instead of a flat dropdown.
2. Fix is two-line: add "droplink" to the CanHandle word list (line 23) and to the inner gate at line 83. Both use HasWord (substring IndexOf), so no ambiguity with "droplist" or "groupeddroplink" - the existing branch order (groupeddroplist > groupeddroplink > LookupEx fallthrough) still correctly routes each editor.
3. Return type intentionally unchanged: droplink returns an Item via LookupEx's existing ReadValue path (line 241-247), matching Add-Droplink callers (Remoting API Key Insert/Edit dialogs) that already consume Item.
4. No TDD red phase - no unit-test harness for ListVariableEditor (requires live Sitecore runspace); verified via Debug build + manual Remoting API Key dialog testing.

**Context Note - Remoting auth terminology (added mid-branch):**
1. Decision: config-based shared-secret auth is NOT deprecated; it is the intended mechanism for trusted automation (CI/CD pipelines, infrastructure services). API Keys are intended for public consumers (MCP server, third-party integrations, individual developers). Explicitly not rate-limited by design on the config-based path.
2. Renamed "legacy" to "config-based" in 6 sites across RemoteScriptCall.ashx.cs and RemotingApiKeyProvider.cs (5 comments + 1 log action string `legacySecretFailed` -> `configSecretFailed`). Log action string change is a minor breaking contract for operators filtering on the old string at Debug level.
3. Added the intent comment at Path B (RemoteScriptCall.ashx.cs:269) documenting the automation vs public-consumer split.
4. Test descriptions in Remoting.RemotingPolicies.Tests.ps1 updated to match; function name Invoke-LegacyRequest and variable $legacyResponse kept (code identifiers, not descriptions - rename is a refactor out of scope).

## Current Task: ConvertFrom-CliXml DTD hardening

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Failing Test
- [x] Implemented
- [x] Documented (test file comments + worklog)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/clixml-dtd-hardening (branched from release/9.0)
**Last Success:** task build + task test (376 / 0 fail / 12 skip, including 10 new DTD regression tests now green). Unit run: 239 passed / 0 failed / 3 skipped.
**Next Step:** Manual verification - run a remoting round-trip (Invoke-RemoteScript) to confirm ConvertFrom-CliXml deserialization still works end-to-end, then --no-ff merge into release/9.0.

**Severity re-scoped after empirical measurement (2026-04-18):**
Original framing ("billion-laughs DoS") overstated the impact on this stack.
Measured behavior of the pre-fix code on .NET Framework 4.8:
- XmlTextReaderImpl has a hardcoded MaxCharactersFromEntities = 10,000,000,
  so catastrophic memory exhaustion cannot occur; a depth-7 fan-out-10
  payload (30M expected chars) throws "The input document has exceeded a
  limit set by MaxCharactersFromEntities" after ~1s.
- Depth 6 (3M chars) deserializes successfully in ~1.15s and allocates
  ~6 MB of string per request. Depth 5 (300K chars) completes in ~166ms.
  This is a real but sub-catastrophic CPU/allocation amplification: a
  ~700-byte payload forces ~1s CPU and several MB of RAM per request.
- Single-entity substitution works silently (`<!ENTITY xxe "PWNED">` +
  `<S>&xxe;</S>` deserializes to "PWNED"), confirming DTD processing is
  active on an endpoint that never legitimately receives a DOCTYPE.
- The right fix is still at the API layer (DtdProcessing = Prohibit)
  rather than relying on an internal framework cap that could move in a
  future .NET update.

**Context Note:**
1. Fix applied to all 4 locations: modules/SPE/ConvertFrom-CliXml.ps1:21 (now lines 21-24), src/Spe/Commands/Remoting/ConvertFromCliXmlCommand.cs:24 (now lines 22-26 + swap at 31), serialization/modules/serialization/SPE/SPE/Core/Platform/Functions/Remoting.yml:78 (now 78-81), serialization/modules/serialization/SPE/SPE/Core/Platform/Functions/Remoting2.yml:550 (now 550-553). Replacement uses `XmlReader.Create(sr, settings)` with `DtdProcessing = Prohibit` and `XmlResolver = null`.
2. YAML edits used sed with capture-group `^([[:space:]]*)\$xr = New-Object...` to preserve each file's indent level (8 vs 12 spaces). Edit tool was avoided per the YAML blank-line rule; awk verified no empty lines introduced.
3. Test design: runtime malicious-payload test on the PS function (single-entity `<!ENTITY xxe "PWNED">` + `<S>&xxe;</S>`), plus source-regression guards on all 4 files. C# cmdlet gets source-guard coverage only because runtime invocation requires a Sitecore-hosted runspace. Red phase confirmed the vulnerability: pre-fix output was exactly "PWNED" with no warning or exception.
4. Post-fix behavior: on DTD payload the parser throws XmlException during ctor.Invoke or Done() (not inside the inner try-catch around Deserialize) - the exception propagates out of the cmdlet/function. Test assertion tolerates either "warning emitted" or "exception thrown" to stay robust against internal behavior shifts.

## Current Task: Invoke-RemoteScript API-key feature gaps (auto-retry + observability + diagnostics)

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created (feature/remoting-client-api-key-gaps)
- [x] Failing Tests (unit + integration, per-gap red phase)
- [x] Implemented (all 4 gaps green)
- [x] Documented (inline comments at server/client boundaries)
- [ ] Committed
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/remoting-client-api-key-gaps (branched from release/9.0)
**Last Success:** task deploy + task test = 408 passed / 0 failed / 11 skipped (Total 419). New coverage: 14 unit tests in SPE.ClientRetry.Tests.ps1, 20 integration tests in Remoting.ClientRetry.Tests.ps1 (Phase 6 added to Run-RemotingTests.ps1).
**Next Step:** Manual verification - run a few remote calls with/without -MaxRetries against the expired/disabled keys, confirm the friendlier error messages. Then squash merge into release/9.0 and push after AskUserQuestion.

**Scope decisions confirmed with user:**
1. One branch squashed, all 4 gaps in a single commit on merge.
2. Gap 4 (rate-limit on success) surfaced via Write-Verbose only (no property-on-return / no script-scope variable). Opt-in via -Verbose.
3. Gap 3 auth-failure signal uses ONE new response header: `X-SPE-AuthFailureReason: expired|disabled|invalid`. `invalid` deliberately collapses unknown-kid and signature-mismatch to resist enumeration.
4. Transient 5xx retry (500/502/504) dropped from scope - non-idempotent scripts could be double-applied. Only 429 and 503 retry (both provably pre-execution).

**Implementation notes:**
1. Client (modules/SPE/Invoke-RemoteScript.ps1): added `-MaxRetries` param (ValidateRange 0..10, default 0). Existing try/catch wrapped in a `do { ... } while ($true)` loop that honors Retry-After on 429 (60s ceiling) and 503 (10s ceiling). 401 branch reads `X-SPE-AuthFailureReason` header and emits a category-specific message. On success, rate-limit headers emitted via Write-Verbose when present.
2. Server (src/Spe/sitecore modules/PowerShell/Services/RemoteScriptCall.ashx.cs): tracks `authFailureReason` local through Path A (kid present), sets it to `invalid` on signature mismatch or sets it from `RemotingApiKeyProvider.GetAuthFailureReason(kid)` for unknown/expired/disabled paths. Header emitted at both the normal and exception rejection sites.
3. Provider (src/Spe/Core/Settings/Authorization/RemotingApiKeyProvider.cs): `ParseApiKey` no longer drops expired entries - it returns them with their Expires set so `IsExpired` is true. `FindByAccessKeyId` adds `&& !key.IsExpired` to filter at lookup. A parallel cache `KidStatesCacheKey` records disabled/expired state per AccessKeyId for `GetAuthFailureReason` lookups. Expired takes precedence over Disabled when both apply.
4. YAML serialized Invoke-RemoteScript (serialization/.../Remoting2.yml) NOT mirrored - that copy is the legacy asmx/New-WebServiceProxy path, not HttpClient, so there's no 429/503/401 handling to update. Scope was limited to the modules/SPE/ client.

**Test coverage:**
- Unit (tests/unit/SPE.ClientRetry.Tests.ps1): 14 tests using a mocked HttpMessageHandler that simulates 429/503/success responses. Covers retry count, Retry-After honoring, 503 ceiling, and verbose emission on success. No Sitecore container required.
- Integration (tests/integration/Remoting.ClientRetry.Tests.ps1 + Setup/Teardown + Phase 6 in Run-RemotingTests.ps1): 20 tests hitting a real container. Covers 429 retry behavior against a real throttled API Key (RequestLimit=1, Window=3s), success-path verbose, and X-SPE-AuthFailureReason for expired/disabled/unknown scenarios from both raw HttpClient and Invoke-RemoteScript.
- Integration tests check captured error stream via `($errs | ForEach-Object { $_.ToString() }) -join " | "` rather than `$errs[0]` because the PS .NET method-invocation wrapper can land in the error variable alongside my Write-Error call.

**TDD rule for this entry:** Every gap below requires a failing test (Pester integration or unit) landing BEFORE the corresponding fix. No behavior change without a red test that proves the gap exists today. This applies to each individual gap - don't bundle the fixes ahead of their tests. Test sketches listed per-gap below.

**Planned branch:** feature/remoting-client-api-key-gaps (scheduling TBD)

**Observed behavior today:**
Invoke-RemoteScript.ps1:407-428 reads Retry-After, X-RateLimit-Limit, and X-RateLimit-Reset headers on HTTP 429, embeds the values in a Write-Error message ("Rate limit exceeded. Limit: N... Retry after N seconds."), sets $encounteredError, and propagates. No Start-Sleep, no retry loop, no backoff. The only Start-Sleep calls in the module are connection warm-up (line 199) and polling in Wait-* / Stop-ScriptSession - all unrelated. Beyond 429, the client also lacks handling for 503 cold-start retries, 401 expired-key diagnostics, success-path rate-limit visibility, and X-RateLimit-Remaining parsing.

**Gap 1 - Auto-retry on 429 (known)**
- Rough direction: opt-in retry via a new -MaxRetries / -RespectRetryAfter parameter set defaulting safely; wrap the send in a for-loop that honors Retry-After on 429, capped at a sane ceiling (e.g. 60s) to prevent a hostile server parking the client; also retry transient 5xx with exponential backoff.
- TDD red test sketch: use a real API Key with RequestLimit=1/window, issue two requests back-to-back with -MaxRetries=1. Today: second request fails with "Rate limit exceeded" error. After fix: second request sleeps Retry-After seconds then succeeds. Test asserts final status and total elapsed >= Retry-After.

**Gap 2 - Cold-start 503 auto-retry (medium)**
- Server emits 503 + Retry-After: 2 at RemoteScriptCall.ashx.cs:244-246 when the API key registry hasn't loaded yet. Client has no 503 branch; first-request-after-restart fails with a generic error instead of transparently waiting and retrying. This is the one case the server explicitly asks the client to auto-retry.
- Fix direction: add a 503 branch parallel to the 429 branch. Honor Retry-After (default 2s if missing). Shorter ceiling than the 429 case - cold start is expected to resolve in seconds, not minutes.
- TDD red test sketch: restart the CM container (or force an authorization-cache invalidation), then issue a request immediately. Today: 503 returns as a generic connection error. After fix: client sleeps and retries transparently, request succeeds on retry. If container restart is too heavy for integration tests, a mock HTTP server that returns 503 once then 200 works instead.

**Gap 3 - 401 expired-key diagnostics (low-medium)**
- Server rejects expired keys with 401 (integration test "[PASS] Expired key is rejected (401)" at line 1522 confirms this). Client's catch [HttpRequestException] block only handles 429 and 403. 401 falls through to the generic error path; admins debugging expired keys see "Server response: Unauthorized" rather than "API Key expired".
- Fix direction: add a 401 branch. Inspect response body (server may include a reason payload) and/or WWW-Authenticate header. If the server doesn't currently surface the expiration reason to the client, add a server-side header (e.g. X-SPE-AuthFailureReason: expired|disabled|signatureInvalid) and handle it on the client.
- TDD red test sketch: create an API Key with Expires=past-date, issue a request with its shared secret. Today: error message is generic "Unauthorized". After fix: error message says "API Key expired on <date>" or equivalent, with a distinguishable ErrorCategory (AuthenticationError vs ConnectionError).

**Gap 4 - Rate-limit headers on successful responses not surfaced (low, observability)**
- Server emits X-RateLimit-Limit / -Remaining / -Reset on every API-key-authenticated request (success or 429) via AddThrottleHeaders at line 141. Client only reads them inside the 429 error branch. Successful callers never see remaining budget, can't preemptively slow down.
- Fix direction: on successful response, read the rate-limit headers and expose them. Two options:
  - Attach to the returned object as properties (works for Invoke-RemoteScript return value shape).
  - Emit via Write-Verbose -Message when the headers are present (non-intrusive, opt-in via -Verbose).
- Decision needed in Phase 1: changing return shape is a breaking contract; Write-Verbose is safe but less discoverable.
- TDD red test sketch: issue a successful request with an API Key that has RequestLimit=60. Today: no rate-limit info reaches the caller. After fix: caller can read (via property or -Verbose capture) Limit=60, Remaining=59, Reset=<timestamp>. Integration test asserts values match what the server sends.

**Gap 5 - X-RateLimit-Remaining dropped on the floor (trivial)**
- Even in the 429 branch, Invoke-RemoteScript.ps1:414-418 reads only X-RateLimit-Limit and X-RateLimit-Reset. X-RateLimit-Remaining is never parsed, so the error message says "Limit: M" instead of "exceeded by N over cap M".
- Fix direction: one-line add of the header read + message concat. Trivially bundled with Gap 1 and Gap 4.
- TDD red test sketch: exceed the limit on a throttled API Key with a known Limit. Today: error message does not contain the word "Remaining" or the remaining count. After fix: error message surfaces remaining=0 (or the overshoot count).

**Open questions that require research before implementation (apply to Gaps 1 and 2):**
1. Default on or off? Auto-retry can mask abuse and hide client bugs; defaulting off preserves current behavior but fewer consumers benefit. Need to check how other SPE clients (MCP server) expect to handle 429/503 today.
2. Which status codes should trigger retry? 429 clearly. 503 clearly (server asks for it). 502/504 maybe. What about 500? (Generally no - retry on 500 can amplify real server bugs.)
3. Retry-After parsing: header can be either seconds (integer) or an HTTP-date. Today's code treats it as a string; a robust implementation needs to handle both forms.
4. Ceiling on Retry-After honoring - hostile server could send Retry-After: 86400. 60s for 429, maybe 10s for 503 as starting suggestions; confirm with operators.
5. Idempotency concern: retry is safe for read-only scripts but dangerous for scripts that mutate state. Caller-script semantics are opaque to the client. Either restrict retry to known-safe operations (file download, GET-style requests) or document that enabling retry means the caller owns idempotency.
6. Interaction with session-persistence (-PersistentSession). If a retry lands after the session has expired, behavior diverges. Needs test coverage.
7. Backoff algorithm: fixed Retry-After, exponential with jitter, token bucket? Exponential + jitter is standard; Retry-After should be honored as a lower bound, not overridden.
8. Cancellation: long retry sleeps need to honor Ctrl+C / cancellation tokens.
9. Test harness: need a way to simulate 429/503 responses in integration tests. Today's throttle tests exercise the server side; a client-retry test needs either a mock HTTP server or a real throttled API Key with very low limits to trigger 429 predictably, and (for 503) either a cold-start orchestration or a mock.

**Additional open question for Gap 3:**
10. Does the server currently surface expiration reason in a consumable form, or does this require a matching server change (new header X-SPE-AuthFailureReason)? If server change is needed, scope both sides in the same branch to avoid a partial-rollout window.

**Additional open question for Gap 4:**
11. Return-shape change vs Write-Verbose: which do consumers prefer? Check how the MCP server and any existing Invoke-RemoteScript callers consume the return value today. A property-bag attachment could break strict-typed consumers.

**Why not yet scheduled:** Design questions above have non-obvious answers that depend on consumer expectations (MCP server, CI/CD pipelines, interactive ISE). Worth a short research spike before committing to a plan. Gaps 1, 2, 3 need design; Gaps 4 and 5 are smaller and could ship earlier if scoped separately.

**Other deferred security hardening items (not yet scheduled):**
- Script execution timeout (per-policy, cooperative via PowerShell.Stop + timer, NOT Task.Run - context switch loses HttpContext / ThreadStatic Runspace.DefaultRunspace / ambient DatabaseSwitcher).
- Request-size cap (maxRequestLength / maxAllowedContentLength) scoped to the remoting handler location only - file-upload cmdlets must still work.
- Session / runspace count cap per identity (needs runspace-vs-session accounting design - a single session can spawn many runspaces via background jobs).
- Unbounded remoting parameter binding size cap (RemoteScriptCall.ashx.cs:1228-1264).
- Slowloris: recommended applicationHost.config snippet in deployment docs (SPE can't fix at code layer).
- _throttleState LRU eviction if non-global throttle scopes ever get enabled.

**Deferred OAuth bearer feature (parked, OAuth surface already large):**
- Config-only OAuth Clients (analogous to SharedSecret config-secret path).
  Today every accepted OAuth bearer token MUST match an OAuth Client content
  item via (iss, client_id); the item supplies ImpersonateUser, Policy,
  Expires, Enabled, throttle bucket. Rationale to add: bootstrap (IdP ready
  before items exist), ops teams who require XML-only auth config, DR when
  master DB is unreachable. Rationale against: needs a config-side
  client_id -> ImpersonateUser/Policy map (gets ugly fast); loses the item's
  Enabled/Expires/audit trail; loses per-client throttle keying; adds a
  third auth surface to maintain. Lighter-weight stepping stone if a real
  use case shows up: support `client_id="*"` wildcard on an OAuth Client
  item ("any caller from trusted issuer X gets this impersonation"). Don't
  build the full config-only mode unless someone hits the specific bootstrap
  or DR scenario.

**Deferred UX / compat items flagged during the stream-fix branch (not yet scheduled):**
- **Wait-RemoteSitecoreJob is incompatible with ConstrainedLanguage policies.** The polling scriptblock calls `[Sitecore.Jobs.JobManager]::GetJob(...)` and `[Sitecore.Handle]::Parse(...)` - both are .NET static method invocations that ConstrainedLanguage blocks regardless of AllowedCommands. Works today in FullLanguage policies only. Silently broken in Constrained: the first poll fails the static-method guard and no allowlist fix helps. Options: (a) document "FullLanguage required" on the cmdlet and error cleanly when the policy is Constrained, (b) provide a server-side helper cmdlet (Get-SitecoreJobStatus or equivalent) that can be allowlisted and called from the polling script so no static calls are needed, (c) route the polling through an existing allowlisted cmdlet like Get-ScriptSession + a new job-status adapter. (b) is the right long-term answer. Related: Wait-RemoteScriptSession polling script uses `Get-ScriptSession` + `Receive-ScriptSession`, which work in ConstrainedLanguage but need to be in AllowedCommands on any restrictive policy - this is a policy-configuration concern, not a SPE code bug.

## ISE UX improvements (clear-output button + splitter ratio preservation)

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Implementation
- [x] Committed (550f3bfc3 clear-output, a0bb1d010 splitter ratio, cb5418153 viewport clamp)
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/ise-clear-output-button (propose rename to feature/ise-ux-improvements at merge)
**Last Success:** Commit cb5418153 - viewport-bound clamp so ScriptPane can't push StatusBar off-screen on small windows
**Next Step:** Await manual testing approval, then merge into release/9.0 with --no-ff
**Context Note:**
1. Clear button: #ClearOutput icon (office/16x16/erase.png) placed between ToggleSplitOrientation and ShowHideResults; click calls existing spe.clearOutput().
2. Splitter ratio: new spe.reapplySplitPosition() called from spe.resizeEditor reapplies stored ratio on every resize. Vertical was already ratio-based; horizontal changed PsHSplitter to save ratio instead of pixels. No backwards-compat migration - stale pixel values just clamp. Asymmetric clamp on resize: editor floor 200px, output floor 100px, editor wins on conflict. Drag-time 25% symmetric clamp unchanged.

## Completed: Refactor PowerShellMultiValuePrompt - Editor Registry Pattern

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Implementation
- [ ] Testing
- [ ] Manual Testing Approved
- [ ] Phase 3: Merged

**Session ID:** feature/dialog-builder-improvements
**Last Success:** Build passes. 17 new files created (IVariableEditor + context + helper + 14 editors). PowerShellMultiValuePrompt reduced from ~1467 to ~660 lines. DialogBuilder MaxLength param added. Ace snippets updated with spe-dialogmaxlength and kitchen sink MaxLength examples.
**Next Step:** Manual testing with Read-Variable scripts covering each editor type. Commit when approved.
**Context Note:** Registry pattern in Editors list. Order matters - TextVariableEditor is fallback (last). MemoVariableEditor must precede options-based editors.

## Completed: Add TTL to DelegatedAccessManager cache (#1446)

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Implementation
- [x] Testing
- [x] Manual Testing Approved
- [x] Phase 3: Merged

**Session ID:** feature/delegated-access-cache-ttl
**Last Success:** Merged into release/9.0
**Next Step:** None — complete
**Context Note:** Added CachedAccessEntry wrapper with ExpiresUtc around DelegatedAccessEntry in ConcurrentDictionary. Cache entries expire after configurable TTL (Spe.DelegatedAccessCacheTtlSeconds, default 300s). Lazy cleanup runs every 60s during cache reads. Event-based invalidation via DelegatedAccessMonitor still works immediately. Negative (denied) entries also get TTL to prevent stale denials.

## Completed: Fix SecureCompare length leak (#1452)

- [x] Phase 1: Plan Approved
- [x] Phase 2: Branch Created
- [x] Implementation
- [x] Testing
- [x] Manual Testing Approved
- [x] Phase 3: Merged

**Session ID:** feature/secure-compare-length-leak
**Last Success:** Pushed to origin/release/9.0
**Next Step:** None — complete

## Completed: Reorder JWT validation — signature before claims (#1450)

- [x] Plan Approved
- [x] Branch Created
- [x] Implementation
- [x] Testing
- [x] Documentation
- [x] Merged

**Session ID:** feature/jwt-signature-first
**Last Success:** Committed 9063d8b18 — all 10 new tests pass (9 PASS + 1 SKIP)
**Next Step:** Await manual testing approval, then merge into release/9.0
**Context Note:** Moved signature verification (ComputeHash + IsValidSignature) before all claim checks (exp, nbf, iat, lifetime, audience, issuer) in ValidateToken. No functional change for valid tokens — only invalid tokens now fail earlier at signature instead of leaking claim validation results.

## Feature: Fix race condition in API key authentication (#1439)
**Branch:** `feature/race-condition-api-key-auth` from `release/9.0`
- [x] Committed | [x] Merged

## Feature: Fix ISE keyboard shortcuts blocked by Ace editor (#1457)
**Branch:** `feature/ise-keyboard-shortcut-overrides` from `release/9.0`
- [x] Committed | [x] Merged

## Feature: Convert Restriction Profiles to Allowlist & Remove ItemPathRestrictions
**Branch:** `feature/allowlist-restriction-profiles` from `release/9.0`
- [x] Committed | [x] Merged

## Feature: Standardize Logging Format (#1435)
**Branch:** `feature/standardize-logging` from `release/9.0`
- [x] Committed | [x] Merged

## Feature: Delegated Access Quick Fixes (#1434)
**Branch:** `feature/delegated-access-fixes` from `release/9.0`
- [x] Committed | [x] Merged

## Feature: Version-Specific Serialization Abstraction
**Branch:** `feature/version-specific-serialization` from `release/9.0`
- [x] Implementation | [ ] Committed | [ ] Merged
**Next Step:** Commit changes, then await manual testing before merge.
